# ---- MedQuize: use only what you need from your existing env ----

# MongoDB (required) – use either full URI or URL + DB
# Option A: full URI including database name
# MONGODB_URI=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/MedQuize?retryWrites=true&w=majority
# Option B: URL + DB (e.g. same as MedSherpa)
MONGODB_URL=mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
DB=MedQuize
# If you get querySrv ECONNREFUSED, use Direct connection from Atlas and set:
# MONGODB_DIRECT_URI=mongodb://USER:PASSWORD@cluster0-shard-00-00.xxxxx.mongodb.net:27017,cluster0-shard-00-01.xxxxx.mongodb.net:27017,cluster0-shard-00-02.xxxxx.mongodb.net:27017/MedQuize?ssl=true&replicaSet=atlas-xxxxx&authSource=admin

# Quiz generation – tried in order: Mistral → Groq → template
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-tiny
# GROQ_API_KEY=

# Generate notes from topic (use at least one)
# ChatGPT: https://platform.openai.com/api-keys
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Gemini (free tier): https://aistudio.google.com/app/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# Auth (optional – for future login/signup)
JWT_SECRET=your_secret_here
JWT_ALGO=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=3200

# Email (for password reset – link sent to registered email; we also note mobile in the email)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
FROM_EMAIL=

# Admin / CORS (optional)
ADMIN_EMAIL=
ALLOWED_ORIGINS=*
